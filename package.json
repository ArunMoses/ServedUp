{
  "name": "servedup",
  "version": "1.0.0",
  "description": "cypress tests",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ArunMoses/ServedUp.git"
  },
  "author": "arun moses",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ArunMoses/ServedUp/issues"
  },
  "homepage": "https://github.com/ArunMoses/ServedUp#readme",
  "dependencies": {
    "cypress": "^5.3.0",
    "cypress-cucumber-preprocessor": "^3.0.0",
    "cypress-xpath": "^1.6.0"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true
  },
  "scripts": {
    "test-desktop": "cypress run --browser chrome --config-file cypress-desktop.json",
    "test-tablet": "cypress run --browser chrome --config-file cypress-tablet.json",
    "test-mobile": "cypress run --browser chrome --config-file cypress-mobile.json"
  }
}
